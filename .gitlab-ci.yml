before_script:
  - chmod +x ./configure
  - ./configure
  - make
  - make install
test:
  script:
    - chmod +x ~/.profile
    - . ~/.profile
    - ./bin/test
    # TOFIX "tput: No value for $TERM and no -T specified"