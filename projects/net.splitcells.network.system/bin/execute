#!/usr/bin/env sh
# SPDX-License-Identifier: EPL-2.0 OR GPL-2.0-or-later
# SPDX-FileCopyrightText: Contributors To The `net.splitcells.*` Projects
mvn clean compile && mvn exec:java -Dexec.mainClass=net.splitcells.network.worker.via.java.Builder "-Dexec.args=$(hostname)"
    # The command hostname instead of the environmental variable hostname is used, because it is more portable.
