export function advertiseInterestingThirdParty() {
    var interestingThirdParties = [
        {
            }
        ];
    return interestingThirdParties[Math.floor(Math.random() * interestingThirdParties.length)];
}