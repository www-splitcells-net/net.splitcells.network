<?xml version="1.0" encoding="UTF-8"?>
<article xmlns:xlink="http://www.w3.org/1999/xlink"
         xmlns="http://splitcells.net/sew.xsd"
         xmlns:d="http://splitcells.net/den.xsd">
    <meta>
        <!--
            SPDX-License-Identifier: EPL-2.0 OR GPL-2.0-or-later
            SPDX-FileCopyrightText: Contributors To The `net.splitcells.*` Projects
        -->
        <title>Python Guidelines</title>
    </meta>
    <paragraph>Python 3 is used instead of shell,
        when otherwise somewhat complex logic would be done in shell scripts or argument parsing needs to be done.
        Minimize the number of templates inside a Python script,
        in order to simplify the code.
        In the ideal case, there is only one.
        Such an ideal Python script is basically like a simple shell script,
        where all arguments are passed implicitly as environment variables,
        but argument parsing is done explicitly via program arguments.
    </paragraph>
    <paragraph>
        Use dedicated documentation files for detailed documentation.
        Help messages in CLI commands should only describe the minimal interface and
        show how more detailed information can be obtained.
    </paragraph>
    <paragraph>
        Python code should adhere to <link url="https://peps.python.org/pep-0008">PEP 8 – Style Guide for Python Code</link>,
        as much as reasonable.
    </paragraph>
</article>